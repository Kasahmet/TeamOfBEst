package tatiana.week_01;

public class DivideWithoutOperator {
    public static void main(String[] args) {
        int a = 22;
        int b = 10;

        System.out.println(Math.floorDiv(a,b));

        /* Math.floorDiv(a, b): The Math.floorDiv method is a part of the Math class in Java, and it performs integer division.
        It takes two arguments (`a` and `b` in this case) and returns the largest(closest to positive infinity) integer that is less than or equal to the
        algebraic quotient. In other words, it performs division and rounds down to the nearest integer.
         */
    }
}
/*Numbers -- Divide without / operator
Write a method that can divide two numbers without using division operator.
*/


